# P2-Magnetic-Imaging-Tile — System Specification

**Status:** Stub — content pending. See `DOCs/PUNCH-LIST.md` item "Write the system-wide project specification."

This document will hold the project-wide architectural target: scope, performance targets, the sensor → decimation → display pipeline, cog allocation, data flow, and inter-subsystem interfaces.

Until this is written, the per-subsystem theory-of-operations docs in `DOCs/Theory-of-Operations/` and the architecture docs in `DOCs/Architecture/` serve as the closest substitute.
